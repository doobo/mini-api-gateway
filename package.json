{
  "name": "mini-api-gateway",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "build": "bun build src/index.ts --compile --outfile dist/mini-api-gateway",
    "check": "tsc --noEmit",
    "release": "bun scripts/release.ts"
  },
  "dependencies": {
    "hono": "^4.10.7",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.0"
  }
}
